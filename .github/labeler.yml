# Add 'demo' label to any app file changes
  demo:
     - ./apps/*

# Add 'state' label to any change within the 'state' package
  state:
     - libs/state/*
     - libs/state/**/*

# Add 'template' label to any change within the 'template' package
  template:
     - libs/template/*
     - libs/template/**/*

# Add 'documentation' label to any change to files within a docs dir
  documentation:
    - libs/**/docs/**/*
    - docs/**/*

# Add 'test' label to any change to *.spec.js files within the source dir
  test:
     - libs/**/*.spec.js
     - libs/**/perf/**/*

# Add 'root-file-changes' label to any change to files located directly in the root dir
#  root-files-affected:
#    - '*'
#    - '!*.*'

# Add 'github' label to any change to files located directly in the root dir
  github:
    - '.github/*'
