import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'virtual-placeholder',
  template: ``,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class VirtualPlaceholder {}
