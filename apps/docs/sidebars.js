/**
 * Creating a sidebar enables you to:
  - create an ordered group of docs
  - render a sidebar for each doc of that group
  - provide next/previous navigation
  The sidebars can be generated from the filesystem, or explicitly defined here.
  Create as many sidebars as you want.
 */

// @ts-check

/**
 * One scoped sidebar per package, each generated from inside its own folder so
 * the sidebar shows only that package's sections. Plus a site-level `concepts`
 * sidebar — the cross-package Explanation home per ADR-RXA-0004 (hybrid IA).
 */

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const pkg = (label, dirName) => [
  {
    type: 'category',
    label,
    items: [{ type: 'autogenerated', dirName }],
  },
];

const sidebars = {
  state: pkg('@rx-angular/state', 'state'),
  template: pkg('@rx-angular/template', 'template'),
  cdk: pkg('@rx-angular/cdk', 'cdk'),
  isr: pkg('@rx-angular/isr', 'isr'),
  eslintPlugin: pkg('@rx-angular/eslint-plugin', 'eslint-plugin'),
  learn: [
    {
      type: 'category',
      label: 'Learn',
      link: { type: 'generated-index', slug: '/learn' },
      items: [{ type: 'autogenerated', dirName: 'learn' }],
    },
  ],
  concepts: [
    {
      type: 'category',
      label: 'Concepts',
      link: { type: 'generated-index', slug: '/concepts' },
      items: [{ type: 'autogenerated', dirName: 'concepts' }],
    },
  ],
};

module.exports = sidebars;
